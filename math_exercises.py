# time converter
import math

seconds = 4500
minutes = seconds // 60
hours = minutes // 60
minutes = minutes % 60
print(f"{seconds} sekundit on {hours} tund(i) ja {minutes} minut(it).")

#student helper


rad = math.radians(angle)
sine = math.sin(rad)
cosine = math.cos(rad)
sine = round(sine, 1)
cosine = round(cosine, 1)
print(cosine)

#greetings

lots_of_heys = ("Hey") * n

#adding number

num_a = 1
num_b = 23
string_number = str(num_a) + str(num_b)


